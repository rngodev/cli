seed: 1
effects:
  users:
    entity: users
    schema:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
  posts:
    entity: posts
    schema:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        author:
          type: reference
          entity: users
